{% assign columns = 12 | divided_by: section.settings.columns %}
<div class="row col-md-10 offset-md-1">
    <div class="col-12">
        {% if section.settings.title %}
            <p class="h4 mb-3">{{ section.settings.title }}</p>
        {% endif %}
    </div>
    {% for project in section.blocks %}
        {% if project.type == "projects" %}
            <a class="col-12 col-sm-{{columns}} box" href="{{ project.settings.url }}">
                <div class="box-image-wrapper" style="background-image: url({{ project.settings.image | img_url: '960x' }})"></div>
                <div class="box-info p-3 text-center col-10 offset-1">
                    <h3 class="h2">{{ project.settings.title }}</h3>
                    <p>{{ project.settings.subtitle }}</p>
                </div>
            </a>
        {% endif %}
    {% endfor %}
</div>


{% schema %}
    {
        "name": "Grid (Large)",
        "class": "container-fluid",
        "tag": "section",
        "settings": [
            {
                "type": "text",
                "id": "title",
                "label": "Section title",
                "info": "Enter title to be shown on top-left",
                "placeholder": "Enter title"
            },
            {
                "type": "range",
                "id": "columns",
                "min": 2,
                "max": 6,
                "step": 1,
                "label": "Columns per Row",
                "default": 2
            }
        ],
        "blocks":[
            {
                "type": "projects",
                "name": "Projects",
                "settings":[
                    {
                        "type": "image_picker",
                        "id": "image",
                        "label": "Project Feature Image"
                    },
                    {
                        "type": "text",
                        "id": "title",
                        "label": "Project Title",
                        "placeholder": "Enter title"
                    },
                    {
                        "type": "text",
                        "id": "subtitle",
                        "label": "Project Subtitle",
                        "placeholder": "Enter subtitle"
                    },
                    {
                        "type": "url",
                        "id": "url",
                        "label": "Project Link",
                        "info": "Select target link"
                    }
                ]
            }
        ]
        
    }
{% endschema %}
